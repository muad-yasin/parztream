[pytest]
pythonpath = .
testpaths = tests
; tests/e2e needs installed Playwright browsers and launches real server
; subprocesses -- kept out of the plain `pytest` run. Run it explicitly
; with:  pytest tests/e2e -o addopts=""
addopts = --ignore=tests/e2e
